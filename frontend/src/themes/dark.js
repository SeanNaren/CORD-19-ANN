import colors from './colors'
const { dark, purple } = colors;

export default {
  palette: {
    type: "dark",
    primary: {
      main: dark,
    },
    secondary: {
      main: purple,
    },
  }
}
