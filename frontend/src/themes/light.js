import colors from './colors'
const { dark, purple } = colors;

export default {
  palette: {
    type: "light",
    primary: {
      main: dark,
    },
    secondary: {
      main: purple,
    },
  },
  typography: {
    body2: {
      fontSize: '0.75rem'
    }
  }
}
