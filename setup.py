from setuptools import setup, find_packages

setup(name='cord_ann',
      version='0.1',
      description='CORD-19-ANN',
      author='Sean Narenthiran',
      packages=find_packages(),
      zip_safe=False)
